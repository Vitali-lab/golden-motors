# Wash Server - Telegram Bot Integration

Сервер для обработки заявок с формы записи на мойку и отправки их в Telegram бота.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd server
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env` в папке `server` (или скопируйте `.env.example`):

```env
TELEGRAM_BOT_TOKEN=8186376750:AAEhEwgUwtBOzd4hiPMGNIcsKuPbBOBTbIQ
TELEGRAM_CHAT_ID=492982383
PORT=3001
```

### 3. Запуск сервера

**Режим разработки (с автоперезагрузкой):**
```bash
npm run dev
```

**Продакшн режим:**
```bash
npm start
```

Сервер запустится на `http://localhost:3001`

## 📡 API Endpoints

### POST `/api/book`

Отправляет заявку на мойку в Telegram бота.

**Тело запроса:**
```json
{
  "name": "Иван Иванов",
  "phone": "+375 (29) 123-45-67",
  "service": "Стандартная мойка",
  "note": "Дополнительная информация (необязательно)"
}
```

**Успешный ответ:**
```json
{
  "success": true,
  "message": "Заявка успешно отправлена! Мы свяжемся с вами в ближайшее время."
}
```

**Ошибка:**
```json
{
  "success": false,
  "error": "Описание ошибки"
}
```

### GET `/health`

Проверка работоспособности сервера.

**Ответ:**
```json
{
  "status": "ok",
  "message": "Server is running"
}
```

## 🔧 Конфигурация

### Переменные окружения

- `TELEGRAM_BOT_TOKEN` - Токен Telegram бота (обязательно)
- `TELEGRAM_CHAT_ID` - ID чата для отправки сообщений (обязательно)
- `PORT` - Порт сервера (по умолчанию: 3001)

## 📱 Формат сообщения в Telegram

Сообщения отправляются в формате HTML с эмодзи:

```
🚗 Новая заявка на мойку

👤 Имя: Иван Иванов
📞 Телефон: +375 (29) 123-45-67
🔧 Услуга: Стандартная мойка
📝 Примечание: Дополнительная информация
⏰ Время: 29.01.2026, 15:30:00
```

## 🛠 Технологии

- **Express.js** - веб-фреймворк для Node.js
- **CORS** - для обработки CORS запросов
- **dotenv** - для работы с переменными окружения
- **Telegram Bot API** - для отправки сообщений

## 🔒 Безопасность

- `.env` файл добавлен в `.gitignore` и не должен попадать в репозиторий
- Используйте переменные окружения для хранения токенов
- В продакшене используйте HTTPS

## 📝 Примечания

- Сервер автоматически валидирует входящие данные
- Телефон должен содержать минимум 9 цифр
- Имя и телефон являются обязательными полями
