# 🚀 Инструкция по установке и запуску

## Шаг 1: Установка зависимостей

### Фронтенд
```bash
npm install
```

### Сервер (ОБЯЗАТЕЛЬНО!)
```bash
cd server
npm install
cd ..
```

**⚠️ ВАЖНО:** Если при установке возникает ошибка сети, попробуйте:
- Проверить интернет-соединение
- Использовать альтернативный registry: `npm install --registry https://registry.npmmirror.com`
- Или использовать yarn: `yarn install`

**Без установленных зависимостей сервер не запустится!**

## Шаг 2: Настройка сервера

Файл `.env` уже создан в папке `server/` с вашими токенами:
- `TELEGRAM_BOT_TOKEN=8186376750:AAEhEwgUwtBOzd4hiPMGNIcsKuPbBOBTbIQ`
- `TELEGRAM_CHAT_ID=492982383`
- `PORT=3001`

Если нужно изменить, отредактируйте файл `server/.env`

## Шаг 3: Запуск проекта

### Вариант A: Запуск по отдельности

**Терминал 1 - Фронтенд:**
```bash
npm run dev
```
Фронтенд будет доступен на `http://localhost:5173`

**Терминал 2 - Сервер:**
```bash
npm run dev:server
```
Сервер будет доступен на `http://localhost:3001`

### Вариант B: Запуск одновременно (требует concurrently)

```bash
# Установите concurrently (если еще не установлен)
npm install --save-dev concurrently

# Запустите все сразу
npm run dev:all
```

## Шаг 4: Проверка работы

1. Откройте браузер: `http://localhost:5173`
2. Заполните форму записи на мойку
3. Отправьте форму
4. Проверьте Telegram - должно прийти сообщение с данными заявки

## 🔍 Проверка сервера

Проверьте, что сервер работает:
```bash
curl http://localhost:3001/health
```

Должен вернуться:
```json
{"status":"ok","message":"Server is running"}
```

## 📝 Формат сообщения в Telegram

Когда форма отправляется, в Telegram приходит сообщение:

```
🚗 Новая заявка на мойку

👤 Имя: Иван Иванов
📞 Телефон: +375 (29) 123-45-67
🔧 Услуга: Стандартная мойка
📝 Примечание: Дополнительная информация
⏰ Время: 29.01.2026, 15:30:00
```

## ⚠️ Устранение проблем

### Сервер не запускается
- Проверьте, что порт 3001 свободен
- Убедитесь, что установлены все зависимости: `cd server && npm install`

### Форма не отправляется
- Проверьте, что сервер запущен на порту 3001
- Откройте консоль браузера (F12) и проверьте ошибки
- Убедитесь, что CORS настроен правильно

### Сообщения не приходят в Telegram
- Проверьте токен бота в `server/.env`
- Проверьте CHAT_ID в `server/.env`
- Убедитесь, что бот активен и вы отправили ему хотя бы одно сообщение

## 🎉 Готово!

Теперь все должно работать. При заполнении формы данные будут отправляться в ваш Telegram бот!
